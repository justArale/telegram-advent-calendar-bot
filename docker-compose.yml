version: '3.8'

services:
  advent-bot:
    build: .
    container_name: advent-calendar-bot
    restart: unless-stopped
    env_file:
      - .env.prod
    volumes:
      - ./logs:/app/logs
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge